import 'package:flutter/material.dart';

class ScreenSize{
  ScreenSize._();
  static const double kHeight=100;
  static const double kWidth=100;
  static const Size kScreenSize=Size(kWidth, kHeight);
}